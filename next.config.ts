import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['lh3.googleusercontent.com', 'encrypted-tbn2.gstatic.com', 'www.fruitsmith.com']
  },
};

export default nextConfig;
